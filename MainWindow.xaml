<Window x:Class="OllamaUsageTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ollama Usage" Height="120" Width="90"
        WindowStyle="None" AllowsTransparency="True" Background="#CC000000"
        Topmost="True" ResizeMode="NoResize" ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="0,5,0,0">
            <TextBlock Text="OLLAMA" FontWeight="Bold"/>
            <Grid Margin="2,2,2,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="txtSession" Grid.Column="0" Text="--%" Foreground="#4CAF50" HorizontalAlignment="Center" FontWeight="Bold"/>
                <TextBlock x:Name="txtWeekly" Grid.Column="1" Text="--%" Foreground="#2196F3" HorizontalAlignment="Center" FontWeight="Bold"/>
            </Grid>
        </StackPanel>

        <!-- Chart area -->
        <Canvas x:Name="chartCanvas" Grid.Row="1" Margin="5" ClipToBounds="True" Background="#22FFFFFF">
            <Polyline x:Name="lineSession" Stroke="#4CAF50" StrokeThickness="1.5" />
            <Polyline x:Name="lineWeekly" Stroke="#2196F3" StrokeThickness="1.5" />
        </Canvas>
    </Grid>
</Window>
