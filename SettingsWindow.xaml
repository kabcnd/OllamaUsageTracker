<Window x:Class="OllamaUsageTracker.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="200" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Cookie Value:" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,5"/>
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtCookie" Height="25" Margin="0,0,5,0" Grid.Column="0"/>
            <Button Content="Login via Browser" Width="110" Height="25" Grid.Column="1" Click="BtnLogin_Click"/>
        </Grid>

        <TextBlock Text="Update Interval (seconds):" FontWeight="Bold" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox x:Name="txtInterval" Grid.Row="3" Height="25" Margin="0,0,0,10"/>

        <CheckBox x:Name="chkDebug" Content="Enable Debug Logging" Grid.Row="4" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="75" Height="25" Margin="0,0,10,0" Click="BtnSave_Click"/>
            <Button Content="Cancel" Width="75" Height="25" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
